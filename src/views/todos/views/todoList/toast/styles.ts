import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: 200,
        height: 70,
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 50,
        backgroundColor: '#fff',
        position: 'absolute',
        alignSelf: 'center',
        top: 50
    },
    text: {
        color: '#000',
        lineHeight: 24,
        fontSize: 20,
    },
}); 
